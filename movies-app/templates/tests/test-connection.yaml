apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "movies-app.fullname" . }}-test-connection"
  labels:
    {{- include "movies-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "movies-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
